<!DOCTYPE html>
<html>
<head>
<style>
    /* Общие стили для всех квадратов */
    .square {
        width: 200px;
        height: 200px;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        transition: all 1s;
    }
    /* Стили для зеленого квадрата */
    .green {
        background-color: green;
        z-index: 2;
    }
    /* Стили для зеленого квадрата при наведении курсора */
    .green:hover {
        width: 300px;
        height: 300px;
        transform: translate(-50%, -50%) rotate(-45deg);
    }
    /* Стили для красного квадрата */
    .red {
        background-color: red;
        top: calc(50% - 50px);
        left: calc(50% - 50px);
    }
    /* Стили для синего квадрата */
    .blue {
        background-color: blue;
        top: calc(50% + 50px);
        left: calc(50% + 50px);
    }
</style>
</head>
<body>

<!-- Зеленый квадрат -->
<div class="square green"></div>
<!-- Красный квадрат -->
<div class="square red"></div>
<!-- Синий квадрат -->
<div class="square blue"></div>

</body>
</html>
